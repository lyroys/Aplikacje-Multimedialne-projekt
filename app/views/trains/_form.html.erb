<%= form_with(model: train, local: true) do |form| %>
  <% if train.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
      <% train.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nazwa %>
    <%= form.text_field :nazwa, id: :train_nazwa %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model, id: :train_model %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
